# Cloudflare Web Analytics beacon token. Public value (rendered into page HTML),
# not a secret. Get it from Cloudflare dashboard > Web Analytics > your site.
# Beacon only loads in production builds (npm run build).
PUBLIC_CF_BEACON_TOKEN=
